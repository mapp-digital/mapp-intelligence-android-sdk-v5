# Consumer R8/ProGuard rules for applications that depend on this SDK.
#
# The SDK currently requires no additional keep rules. Keep this file because
# android-sdk/build.gradle.kts publishes it with both library build types.
